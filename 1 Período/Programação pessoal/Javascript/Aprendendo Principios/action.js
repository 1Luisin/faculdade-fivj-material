// configuração de variaveis 
var numero = 123; // variavel global
let numero = 123; // variavel local
const numero = 123; // variavel estática

// tipagem da linguagem

String;
Number;
CharacterData;

console.log(window.numero) // window 

console.log(nome);
var nome = "Julia";

let y = 32; // variável local pertencente apenas este escopo
var oba = 32; // variavel global por estar fora de uma função
const conteudo_unico = 32; // variável que não pode mudar, constante
variavel_sem_ind = 32; // variavel sem indentificador

function main()
{
    
    var oba1 = 32; // variavel local da funçao "Main"
    let oba2 = 49;
        
        if (oba1 == 32) 
        
            {
                console.log(oba1)
            }
        
        animal = "coelho";
    
    //tipos de variáveis 
    Number;
    String;
    Int32Array;
    Boolean;
    CharacterData;
    

}